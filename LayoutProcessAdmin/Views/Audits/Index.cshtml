@model IEnumerable<LayoutProcessAdmin.Models.Auditing.Audit>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.chr_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.chr_Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.int_Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.chr_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.chr_Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.int_Type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.int_IdAudit }) |
            @Html.ActionLink("Details", "Details", new { id=item.int_IdAudit }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.int_IdAudit })
        </td>
    </tr>
}

</table>
